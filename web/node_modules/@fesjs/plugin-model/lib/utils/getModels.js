"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getModels = getModels;

var _utils = require("@fesjs/utils");

var _ = require(".");

function getModels(cwd, pattern) {
  const files = _utils.glob.sync(pattern || '**/*.{js,jsx,ts,tsx}', {
    cwd
  }).filter(file => !file.endsWith('.d.ts') && !file.endsWith('.test.js') && !file.endsWith('.test.jsx'));

  return (0, _.getValidFiles)(files, cwd);
}