"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _utils = require("@fesjs/utils");

var _default = api => {
  api.describe({
    key: 'checkVuePackage',
    config: {
      schema(joi) {
        return joi.object();
      },

      default: {}
    }
  });
  api.onStart(() => {
    // eslint-disable-next-line import/no-extraneous-dependencies
    const vuePkg = require('vue/package.json');

    const vueCompilerPkg = require('@vue/compiler-sfc/package.json');

    if (!_utils.semver.satisfies(vuePkg.version, `~${vueCompilerPkg.version.replace(/\d+$/, '0')}`, {
      includePrerelease: true
    })) {
      console.log(_utils.chalk.red(`You are using vue@${vuePkg.version}, requires @vue/compiler-sfc@${vuePkg.version}.\nPlease upgrade your @vue/compiler-sfc@${vueCompilerPkg.version} version.`));
      process.exit(1);
    }
  });
};

exports.default = _default;