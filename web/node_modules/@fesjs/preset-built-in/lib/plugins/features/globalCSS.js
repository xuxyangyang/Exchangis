"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _path = require("path");

var _fs = require("fs");

var _default = api => {
  const {
    paths,
    utils: {
      winPath
    }
  } = api;
  const {
    absSrcPath = '',
    absTmpPath = ''
  } = paths;
  const files = ['global.css', 'global.less', 'global.scss', 'global.sass', 'global.styl', 'global.stylus'];
  const globalCSSFile = files.map(file => (0, _path.join)(absSrcPath || '', file)).filter(file => (0, _fs.existsSync)(file)).slice(0, 1);
  api.addEntryCodeAhead(() => `${globalCSSFile.map(file => `require('${winPath((0, _path.relative)(absTmpPath, file))}');`).join('')}`);
};

exports.default = _default;