"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.getAppPath = getAppPath;

var _path = require("path");

var _fs = require("fs");

var _utils = require("@fesjs/utils");

function getAppPath(absSrcPath) {
  for (const suffix of ['.js', '.ts', '.jsm', '.jsx', '.tsx']) {
    const p = (0, _utils.winPath)((0, _path.join)(absSrcPath, `app${suffix}`));

    if ((0, _fs.existsSync)(p)) {
      return p;
    }
  }

  return null;
}