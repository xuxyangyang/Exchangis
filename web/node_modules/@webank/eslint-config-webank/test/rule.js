
const nameSub = 1;

const obj = {};

const _d = 3;

const constants = 1 + 2;

obj[constants] = 1;

console.log(obj);

if (Object.is(nameSub, -0)) {
    // do something
    console.log(_d);
    // require('../index.js');
} else {
    // do other thing
}

function foo() {
    return true;
}
console.log(nameSub);

foo();

const arr = [1, 2];

arr.forEach(async (element) => {
    await foo();
    console.log(element);
});
